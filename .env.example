# ========================================
# MCP Maps 3D - Frontend Configuration
# ========================================
# This file contains REQUIRED configuration for the frontend application.
# Copy this file to frontend/.env.local and fill in your actual API keys.
#
# SECURITY: Never commit .env.local to git! It's already in .gitignore.
#
# See docs/API_KEYS_GUIDE.md for detailed instructions on obtaining these keys.
# ========================================

# ---------------------------------------
# Google Gemini AI (REQUIRED)
# ---------------------------------------
# Purpose: Powers the Scout AI agent for property search and conversational chat
# Get key from: https://aistudio.google.com/app/apikey
# Model used: Gemini 2.5 Flash
# Format: Starts with "AIzaSy"
#
# IMPORTANT: VITE_ prefix is required for Vite to expose this to the browser
# NOTE: process.env.API_KEY in code references this variable
#
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# ---------------------------------------
# Google Maps API (REQUIRED)
# ---------------------------------------
# Purpose: Enables Google Photorealistic 3D Maps rendering
# Get key from: https://console.cloud.google.com/google/maps-apis/credentials
# Required APIs to enable:
#   - Maps JavaScript API
#   - Geocoding API
#   - Directions API
#   - Places API (optional)
# Format: Starts with "AIzaSy"
#
# IMPORTANT: Add API restrictions in Google Cloud Console:
#   - Application restrictions: HTTP referrers (add your domain)
#   - API restrictions: Select only the APIs listed above
#
# For development: Unrestricted is OK
# For production: MUST restrict to your domain(s)
#
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ---------------------------------------
# Supabase Authentication (OPTIONAL - for user authentication)
# ---------------------------------------
# Purpose: Enables user authentication, sign-in, and sign-up features
# Get credentials from: https://supabase.com/dashboard → Project Settings → API
#
# Setup:
#   1. Create a new project at https://supabase.com
#   2. Go to Project Settings → API
#   3. Copy the "Project URL" to VITE_SUPABASE_URL
#   4. Copy the "anon public" key to VITE_SUPABASE_ANON_KEY
#
# If not configured:
#   - Authentication features will be disabled
#   - Users won't be able to sign in/sign up
#   - App will still work for guest users
#
# Free tier: Unlimited API requests, 50,000 monthly active users
#
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ---------------------------------------
# Development Notes
# ---------------------------------------
# - All environment variables MUST use VITE_ prefix to be accessible in the browser
# - Both API keys can use the same Google Cloud project
# - Gemini has generous free tier quotas
# - Google Maps requires billing enabled ($200/month free credit)
# - Supabase has a generous free tier for authentication
# - Monitor usage in respective dashboards to avoid unexpected charges
#
# For help, see:
#   - docs/API_KEYS_GUIDE.md - Complete setup instructions
#   - docs/SECURITY.md - Security best practices
#   - README.md - General project documentation
